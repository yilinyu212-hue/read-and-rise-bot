name: Read and Rise Automation

on:
  workflow_dispatch: # 允许你手动在 GitHub 点击按钮运行
  schedule:
    - cron: '0 2 * * *' # 每天凌晨 2 点自动运行（可选）

jobs:
  run-daily-sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: |
          pip install requests plotly streamlit

      - name: Run Read & Rise Crawler
        env:
          # 🚨 这里映射你在 GitHub Secrets 里的那两把钥匙
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
        run: |
          # 切换到 crawler.py 运行真正的爬虫和 AI 提炼逻辑
          python crawler.py
